<div class="post-details-div">
  <div class="single-post-details border-style">
    <div class="title-details d-flex">
      <p><%= @post.title %> by <%= @post.author.name%></p>
      <div>
        <span>Comments: <%= @post.comments_counter%></span>
        <span>Likes: <%= @post.likes_counter%></span>
      </div>
    </div>
    <div>
      <%= @post.text %>
    </div>
  </div>
  <div class="comments-section border-style">
    <% @post.recent_comments.each do |comment|%>
      <p class="d-flex"><span><%= comment.author.name %>:</span><span><%= comment.text %></span></p>
    <% end %>
  </div>
 <%= render template: 'comments/create', post_id: @post.id %>
</div>
